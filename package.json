{
  "name": "finnish-train-price-calendar",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "prebuild": "rm -rf dist/",
    "build": "parcel build src/client/index.html --public-url ./",
    "dev": "concurrently --kill-others \"yarn parcel ./src/client/index.html\" \"nodemon ./src/server/index.js\""
  },
  "dependencies": {
    "axios": "^0.19.1",
    "express": "^4.17.1",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "concurrently": "^5.0.2",
    "nodemon": "^2.0.2",
    "parcel-bundler": "^1.12.4"
  }
}
